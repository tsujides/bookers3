<% if relationships.present? %>
<table class='table'>
  <thead>
    <tr>
      <th>image</th>
      <th>name</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% relationships.each do |relationship| %>
      <tr>
        <td><%= image_tag relationship.followed.get_profile_image, size: '50x50' %></td>
        <td><%= relationship.followed.name %></td>
        <td>フォロー数: <%= relationship.followed.followers.count %></td>
        <td>フォロワー数: <%= relationship.followed.followeds.count %></td>
        <td><%= link_to 'Show', relationship.followed, class: "user_#{relationship.followed.id}" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<% else %>
ユーザーはいません。
<% end %>
